# 画面構成

メイン画面は図1-1のとおりです。

<img src="images/_home_screen.png" width=400px />



## 1.2. システムステータスバー

<img src="images/_home_screen_status_bar.png" width=480px />

システムステータス表示
: マーキングシステムの動作状態やエラー情報などを表示します。

更新（Refresh）
: 飛行マーキング中に情報をオンライン更新することができます。オンライン更新には2つの方式があります。
1つ目は「システムキャッシュを2回行った後にマーキング内容を更新する」方式です。オンライン更新をクリックすると、システムはもう一度マーキングを実行し、その内容を1回更新します。修正後の内容がマーキングされます。
2つ目は「リアルタイム更新」です。オンライン更新をクリックすると、システムの次回マーキングが修正後の内容になります（高速生産ラインでは、リアルタイム更新により打刻抜けが発生するおそれがあります）。

フォーカスライト（Focus light）
: 赤色フォーカス用の赤色ライトチューブをシステムに接続している場合に使用します。つまり、2本の赤色ライトを接続します。

ガイドライト（Guide light）
: 赤色ガイド光で、データのマーキングエリアをプレビューします。

テスト（Test）
: 現在選択されているデータのマーキング時間をテストします。マーキング後、ステータスバーでマーキング時間を確認できます。

マーキング情報（Marking info）
: ボタンをクリックすると、下記の画面がポップアップ表示されます。
<img src="images/_marking_info.png" width=320px />

内容表示（View content）
: 現在のマーキング内容を表示します。

ファイル名（File name）
: 現在マーキング中のファイル名を表示します。

マーキング時間（Marking time（S））
: 現在のファイルのマーキング時間。

現在回数（Current times）
: スタートコーディングをクリックしてからのマーキング回数をカウントします。

速度（Speed（m/min））
: 現在のエンコーダが取得したラインの実速度、またはシステムで設定したシミュレート速度を表示します。

表示更新（Refresh view）
: マーキング中に、画面上の表示内容をリアルタイムで更新します。

キャッシュクリア（Clear cache）
: オンライン更新機能を使用する際に、マーキング済み内容の以前の状態をリアルタイムでクリアするかどうかを設定します。
チェックされていない場合、システムは2回分のキャッシュ後にマーキングを更新します。システムは内容を2回マーキングし、その後、修正された内容でマーキングを更新します。
チェックされている場合はリアルタイム更新となり、オンライン更新をクリックした後、システムの次回マーキングが修正後の内容になります（高速生産ラインでは、リアルタイム更新により打刻抜けが発生するおそれがあります）。

アラームクリア（Clear alarm）
: アラーム情報をクリアします。

シリアル番号リセット（Reset serial number）
: 生産を停止せずにシリアル番号をリセットできます。

カウントクリア（Clear count）
: 現在回数または総回数をクリアします。

編集（Edit）
: オンライン編集機能です。マーキング中にクリックすると編集画面に戻り、データを修正した後、オンライン更新機能をクリックします。

スタート／一時停止マーキングボタン
: 加工の開始や一時停止を行います。



## 1.3. 編集バー

<img src="images/_home_screen_edit_bar.png" width=480px />



### 1.3.3. 新規

Newボタンをクリックします。これまでに編集したデータを保存するかどうかを確認する画面がポップアップします。保存する場合は［OK］をクリックすると、データファイルの保存画面が表示されます。名前（例：123）を入力し、保存パス「/」にファイルを保存します（図2-45）。
図2-45

#### 1.3.3.1. ファイルを開く

Openボタンをクリックします。システム内部ファイルまたはUSB内に保存されているファイルを開きます。USB内のファイル「123」を選択し、［OK］をクリックします（図2-46）。
図2-46


<img src="./images/_file_open.jpg"  width="300px"/>

Return：一つ前のディレクトリ階層に戻ります。
Open Folder：選択したフォルダを開きます。
New：新規ファイルを作成します。
Delete：ファイルを削除します。
Rename：ファイル名を変更します。
Copy：ファイルをコピーします（USB内でファイルをコピーする、またはシステム内部ファイルをUSBへコピーする際に使用）。
Paste：コピーしたファイルを貼り付けます。

#### 1.3.3.2. 保存

ファイルを上書き保存します。

#### 1.3.3.3. 名前を付けて保存

ファイルに別名を付けて保存します。

#### 1.3.3.4. 元に戻す

直前の操作を取り消します。

#### 1.3.3.5. やり直し

直前に取り消した操作を再実行します。

#### 1.3.3.6. コピー

選択したデータをコピーし、その後、空白領域をクリックすると自動的に貼り付けられます。

#### 1.3.3.7. 削除

データを削除します。


#### 1.3.3.8. ツール

Tool機能には、整列、分布、グループ化、結合、カーブへの変換などが含まれます。

複数のデータがある場合、オブジェクトを上・下・左・右・中央に揃えることができます。
3つ以上のデータがある場合、水平方向の等間隔整列や垂直方向の等間隔整列を行うことができます（図2-47）。
図2-47

Left：選択したオブジェクトを左揃えにします。
Vertical：選択したオブジェクトを垂直方向の中央に揃えます。
Right：選択したオブジェクトを右揃えにします。
Top：選択したオブジェクトの上端を揃えます。
Level：選択したオブジェクトを水平方向の中央に揃えます。
Bottom：選択したオブジェクトの下端を揃えます。

Vertical（分布）：選択したオブジェクトをY方向（垂直方向）に等間隔で分布させます（オブジェクトは3つ以上必要）。
Level（分布）：選択したオブジェクトをX方向（水平方向）に等間隔で分布させます（オブジェクトは3つ以上必要）。

Group：2つ以上のオブジェクトを1つのグループとしてまとめます。
UnGroup：グループ化されたオブジェクトを再び個々のオブジェクトに分解します。

Combination：選択したベクター図形を1つの新しいベクター図形として結合します。
Uncombine：ベクター図形やテキスト内容を、単一のベクター図形（パス）ごとに分解します。

Curve：テキストをベクターパスに変換します。

Obj info：回転オブジェクトの角度を設定し、フォントの重なりを最適化したり、交差部分を削除したりします（交差削除はN_YH1フォントのみ対応）。また、描画・マーキングの順序を最適化します。

例：最適化オーバーラップ値を1に設定した場合、元の属性と最適化オーバーラップを設定した属性は次のように比較されます。
（オーバーラップ未最適化）
（オーバーラップ最適化後）

Array：選択したオブジェクトを複数行・複数列にコピーして配置します（フライトマーキング対応）。




## 1.4. システムツールバー

<img src="./images/_system_toolbar.jpg"  width="300px"/>

Document：スプレー用ファイルを管理します。
ZoomIn：拡大ツール。
ZoomOut：縮小ツール。
Work：マーキングエリア全体を表示します。
Selected：選択中のオブジェクトを最大化表示します。
ALL：すべてのオブジェクトを最大化表示します。


## ?.? 画面右側のパネル


精度（Precision）
: 上下左右、またはロータリーボタンを1ステップ回したときに各ポイントが移動する距離や角度を設定します（単位：mm／deg）。


#### 1.3.3.9. オブジェクト

現在の編集ボックス内に存在するオブジェクト（ファイル）一覧を表示します（図2-48）。
図2-48

Up：選択した項目を上に移動し、マーキング順序を調整します。マーキング順序はリストの上から下へ実行されます。

Down：選択した項目を下に移動し、マーキング順序を調整します（実行順は上から下）。

Topping：選択した項目を最上部（先頭）に移動します。

Bottom：選択した項目を最下部（末尾）に移動します。

On / off marking：選択した項目のマーキングを有効／無効に切り替えます。

Name：選択した項目の名前を変更します。

On / Off Lock：選択した項目のロック／ロック解除を行います。

Delete：選択した項目を削除します。

Layer：3つのデータが同時に存在する場合、それぞれを前のデータのマーキング完了後に順次マーキングするよう順序を設定できます。レイヤーを追加し、各データを1つのレイヤーとして割り当てることで、マーキング順序を変更します。
「Layer management（レイヤー管理）」をクリックすると、図2-49の画面が表示されます。

例：現在の編集ボックスに3つのデータがあり、それらを1つずつ順番にマーキングしたい場合、3つのレイヤーを追加し、各レイヤーに1つずつシリアル番号を割り当てます。各レイヤーの「Start delay（スタートディレイ）」には値（例：1）を設定する必要があります。OKをクリックしてホーム画面に戻ると、図2-50のように、各データで異なるレイヤー番号を選択する必要があります。

Edit：単一のテキストを選択し、そのファイル内容を編集します。
図2-49
図2-50

左図は同一レイヤー内でのマーキング順序、右図は異なるレイヤー間でのマーキング順序を示しています。

#### 1.3.3.10. アークテキスト

Arc textボタンをクリックすると、現在のデータをアーク（弧）状テキストに変更できます（図2-51）。Yesをクリックすると、データはアークテキストになります。
図2-51

Reverse text order：テキストの並び方向（順方向／逆方向）を設定します。

Text style：必要に応じてテキストスタイルを選択します。

Arc radius X：X軸方向の曲線テキストの半径。

Arc radius Y：Y軸方向の曲線テキストの半径。

Starting angle：最初の文字の開始角度。

Limit angle：アーク全体の角度範囲。
例：360度のアークテキストは1周分の円弧テキスト、180度のアークテキストは半円弧テキストとなります（図2-52）。
図2-52

#### 1.3.3.11. 塗りつぶし（Fill）

フォントがダブルラインフォントの場合、または図形が始点と終点がつながったベクター図形の場合、塗りつぶしを行うことができます（図2-53）。
図2-53

Enable fill：チェックを入れると、図形またはフォントの塗りつぶしを有効にします。

Fill angle：塗りつぶし線とX軸との角度を設定します。クイックフィル（図形に対する推奨角度）を選択することもできます。

Line space：塗りつぶし線同士の間隔。

Contour：枠（外形線）を有効にするかどうか。

Fill type：
Optimization：塗りつぶし線をZ字状に走査するなどして、マーキング時間を短縮します。
Ordinary：矢印方向に沿って通常の直線で静的マーキングを行います（図2-54）。
Arch filling：弓形（アーチ状）の軌跡で塗りつぶします。
図2-54

Other parameters：図2-55に示す詳細パラメータを設定します。
図2-55

Evenly distributed：塗りつぶし線の間隔を均等に分配します。

Margin：塗りつぶし線と有効枠との距離。

Straight indent：塗りつぶし線と有効枠とのインデント量。

Boundary ring number：枠線（輪）の本数。

Ring spacing：枠線同士の間隔。

Multiple fills：塗りつぶしを行う回数。

Every offset：前回と次回の塗りつぶし線のオフセット角度。

Distribution value：塗りつぶし線の間隔を任意に分布設定する値。

#### 1.3.3.12. 配列

図2-56のようにオブジェクトを配置します。基準パラメータを設定し、行数・列数およびそれぞれの間隔、並べる方向を設定します。位置の微調整も可能です。設定が完了したら、図2-57のように［Save］をクリックして保存します。
図2-56
図2-57

#### 1.3.3.13. パラメータ

マーキングパラメータに対応するカラーブロックを選択すると、そのマーキングパラメータを直接変更できます。また、設定メニューの「Marking parameters」から変更することもできます。
異なるテキストごとに異なるカラーブロックを選択することも、同じカラーブロックを共有させることもできます。

#### 1.3.3.14. 寸法と座標

図2-58のように、テキストのサイズおよびテキスト座標の位置を変更します。リンクボタンをクリックすると、幅または高さのみを個別に変更することができます。
図2-58

#### 1.3.3.15. ファイル管理

ドキュメント管理：コード印字用のファイルを管理します（下図参照）。

スプレーコードファイル管理：
システム内部に保存されているファイルをUSBメモリにコピーしたり、USBメモリ内のファイルをシステム側にコピーしたりできます。

使用方法：
1.［Internal］をクリックしてファイルを選択し、［Copy］をクリックします。
2.［USB］をクリックし、［Paste］をクリックすると、システム内部ファイルがUSBメモリにコピーされます。



## 1.5. キーボードの説明

キーボード画面は図2-59のとおりです。
図2-59

A：大文字／小文字切り替えキー
B：スペースキー
C：エンターキー
D：カーソルの上下左右移動キー
E：削除、入力方式の切り替え、記号入力
（注：中国語入力はピンイン入力方式です）

Setting：キーボードに表示する入力方式を設定します。キーボードには最大4種類の入力方式を同時に表示できます。
